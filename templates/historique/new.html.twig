{% extends 'Security/Employe_home.html.twig' %}

{% block title %}New Historique{% endblock %}
{% block body %}



<div id="content-wrapper">

<div class="container">
    <div class="card card-login mx-auto mt-5" width="500">
        <div class="card-header">Pointer</div>
        <div class="card-body">
            <div class="text-center mb-4">

            <form>
                <div class="form-group">
                    <div class="form-label-group">
                        {{ form_start(form) }}
                        {{ form_widget(form) }}
                        <button class="btn btn-primary btn-block" >{{ button_label|default('Save') }}</button>
                        {{ form_end(form) }}
                    </div>
                </div>
            </form>
            <div class="text-center">
                <a class="d-block small mt-3" href="{{ path('historique_index') }}">back to list</a>

            </div>
        </div>
    </div>
</div>

<!-- Bootstrap core JavaScript-->


</div>


{% endblock %}
